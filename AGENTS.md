# AGENTS.md

## Project Conventions

- Package manager: use `pnpm` for dependency management and scripts.
- Component library location: `src/components`.
- App foundation: this project uses the TanStack Start template.
- Convex changes: before making any Convex-related changes, review [convex_rules.mdc](.cursor/rules/convex_rules.mdc).
